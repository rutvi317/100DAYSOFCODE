const Courses = (props)=>{
    console.log(props);
    console.log(props.location.pathname)
    return(
       <>
       <h2 style={{color: "black", textAlign: "center"}}>Courses</h2>
       </>
    );
}

export default Courses;